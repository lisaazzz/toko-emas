/**
 * Dashboard.tsx — convenience re-export.
 * The main implementation lives in DashboardTab.tsx.
 * Import from either file; they are the same component.
 */
export { default } from './DashboardTab'
